export interface IAppTheme {
  colors: {
    accent: string;
    accent2: string;
    accent3: string;
    background: string;
    font: string;
    fontAccent: string;
    fontAccent2: string;
    fontAccent3: string;
    shadow: string;
  };
  fonts: {
    title: string;
    text: string;
  };
  iconSizes: {
    small: string;
    medium: string;
    large: string;
    huge: string;
  };
  sizes: {
    mobile: string;
  };
  spacings: {
    line: string;
    tiny: string;
    small: string;
    medium: string;
    large: string;
    huge: string;
  };
  speeds: {
    fast: string;
    normal: string;
  };
}
